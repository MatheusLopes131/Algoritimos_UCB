#include <stdio.h>

int main(){
    int T;
    scanf("%d", &T);
    int N[T];

    for (int i = 0; i < T; i++){
        scanf("%d", &N[T]);
        printf("Fib(%d) = %d", N[T], X);

    }


    return 0;
} /// Incompleto ///